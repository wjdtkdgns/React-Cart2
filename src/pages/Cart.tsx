import CartItems from "../component/CartItems/CartItems";
import Layout from "../component/UI/Layout";

const Cart = () => {
  return (
    <Layout>
      <CartItems />
    </Layout>
  );
};

export default Cart;
